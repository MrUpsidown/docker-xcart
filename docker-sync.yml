version: "2"

options:
  verbose: true
syncs:
  web-sync: # tip: add -sync and you keep consistent names as a convention
    src: './app/xcart'
    # sync_strategy: 'native_osx' # not needed, this is the default now
    sync_excludes: ['.git', 'var']